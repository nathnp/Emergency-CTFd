name: Script Test

on:
  workflow_dispatch:

jobs:
  Test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Prep Test
        run: mv ectfd.sh .. && cd .. && chmod +x ectfd.sh
      - name: Test Script
        run: ./ectfd.sh